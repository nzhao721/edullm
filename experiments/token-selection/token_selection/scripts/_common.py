from __future__ import annotations

# Makes `python -m token_selection.scripts.*` work when PYTHONPATH includes
# experiments/token-selection (repo root: export PYTHONPATH=experiments/token-selection).
